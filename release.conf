# Works with "lorix-os" Yocto distro
OS_DISTRO_VERSION ?= "1.8.2-dev.3"
OS_DISTRO_CODENAME ?= "Niesen"

# Condition the previous systems version has to meet to be upgradable. Examples:
# <=0.5.0
# >0.4.0-rc.2
# >=0.7.0 <0.8.0
#OS_DISTRO_UPGRADE_COMPATIBLE_VERSIONS = "<=$OS_DISTRO_VERSION"

# Packages manager related variables (already defined in lorix-os distro config file)
#OS_DISTRO_FEEDS_BASE_URL = "https://download.iot.wifx.net/lorix-os/"
